<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 查找蓝牙需要开启位置权限 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature android:name="android.hardware.usb.host" />

    <application
        android:name=".CfApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CfTech"
        tools:targetApi="31">
        <activity
            android:name=".lan.ScanLanDeviceActivity"
            android:configChanges="uiMode"
            android:exported="false" />

        <activity
            android:name=".lan.LanDeviceActivity"
            android:configChanges="uiMode"
            android:exported="false" />

        <activity
            android:name=".UsbActivity"
            android:configChanges="screenSize|keyboard|keyboardHidden|navigation|uiMode"
            android:exported="false" />

        <activity
            android:name=".ble.tag_operation.TagOperationActivity"
            android:configChanges="uiMode"
            android:exported="false" />

        <activity
            android:name=".SerialOperatorActivity"
            android:configChanges="uiMode"
            android:exported="false" />

        <activity
            android:name=".ble.BleOperatorActivity"
            android:configChanges="uiMode"
            android:exported="false" />

        <activity
            android:name=".ble.ScanBleActivity"
            android:configChanges="uiMode"
            android:exported="false" />

        <activity
            android:name=".base.BaseActivity"
            android:configChanges="uiMode"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>